# update the version number in `version.rb`, and then run
# `bundle exec rake release` to create a git tag for the
# version, push git commits and tags, and push

module MockWebService
  VERSION = "2.1.0"
end
